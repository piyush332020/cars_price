# cars_price

This project builds a used car price prediction model. It preprocesses the dataset by removing unnecessary columns, duplicates, missing values, and invalid records. It creates a new age feature, converts categorical data into numerical form, and splits the data into training and testing sets. Finally, it trains Linear Regression and Random Forest Regressor models to predict car prices and evaluates their performance using RMSE and R² score.
